#include "underfile.h"

void parser(char *str) {
    int len = strlen(str);
    for (int i = 0; i < len; i++) {

        }

}
    // for (int i = MINUS; i <= MULTIPLE; i++) {
    //     printf("\n%d", i);
    // }